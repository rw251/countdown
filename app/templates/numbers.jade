#numbers-page.numbers-container.page
  .target 000
  .number-grid
    - var n = 1
    while n < 3
      .tile-triplet
        - var m = 1
        while m < 4
          .number-tile
            .tile-wrapper
              .tile.tile-n
              - m++
        - n++
  .pure-g
    .pure-u-1-6
    .pure-u-2-3.number-calc
      .slots
        .tile4
          span.calcslot(data-operator='add') +
        .tile4.tile-end
          span.calcslot(data-operator='subtract') −
        .tile4
          span.calcslot(data-operator='divide') ÷
        .tile4.tile-end
          span.calcslot(data-operator='times') ×
      div
        button#messedUp.pure-button(type='button') Messed it up!!
  .pure-g
    .pure-u-1.number-declare
      form.pure-form
        fieldset
          legend I've got..
          select#number.number-select
            - var n = target-10
            while n <= target+10
              option(value=n, selected=n===target ? '' : undefined)
                = n++
          button#goNumber.pure-button(type='button') Declare!
          button#goNumberNothing.pure-button Nowhere near!
